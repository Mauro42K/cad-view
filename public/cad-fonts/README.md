# Local CAD Fonts

Place an optional private font repository here and point `VITE_CAD_FONTS_BASE_URL` at `/cad-fonts/`.

Expected runtime layout:

```text
public/cad-fonts/fonts/fonts.json
public/cad-fonts/fonts/<font files>
```

Do not commit proprietary font binaries or customer drawing assets.
